# Neon Postgres connection string (pooled). Get it from `neonctl connection-string`.
# NEVER commit the real value — this file is a template only.
DATABASE_URL="postgresql://user:password@ep-xxxx.us-east-1.aws.neon.tech/photobook?sslmode=require"
